using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapView : MonoBehaviour
{
    [SerializeField] Gradient m_numberColors;

    [SerializeField] Sprite m_spriteFlag;
    [SerializeField] Sprite m_spriteMine;
    [SerializeField] Sprite m_spriteLock;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
